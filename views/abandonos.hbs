<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Abandonos por piloto</title>
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
            crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
        <style>
            body {background-color: burlywood;}
        .container {
            display: flex;
            background-color: aquamarine;
            width: 70vh;
            flex-wrap: wrap;
            align-items: center;
            justify-content: center;
            border-style: inset;
        }
        
    </style>
    </head>
    <body>
        {{> navbar}}
        {{!-- <label class="input-group-text" for="inputGroupSelect01">Ubicacion
            de cierre</label>
        <select class="form-select" id="inputGroupSelect01"
            name="ubicacioncierre">

            <option selected>Escoge una...</option>
            {{#each result.resultados}}
            <option value="{{carrera}}">{{carrera}}</option>
            {{/each}}
                  </select>
                </div> --}}

 <div class="container">
                    {{!-- {{#each result.resultados}}
                    <h1>Escudería {{carrera}}</h1>
                    {{/each}}
                    </div>
                     <div class="container">
                    {{#each result.resultados}}
                    <h1>Escudería {{piloto}}</h1>
                    {{/each}}
                    </div> --}}


<div>{{#each result.resultados}}
  <h1>{{this.carrera}}</h1>
  <ul>
    {{#each this.individuales}}
      <li>{{this.piloto}} - Finaliza? {{this.Finaliza}}</li>
    {{/each}}
  </ul>
{{/each}}</div>




        </body>
    </html>